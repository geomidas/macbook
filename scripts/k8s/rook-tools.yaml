apiVersion: v1
kind: Pod
metadata:
  name: rook-tools
  namespace: moon 
spec:
  containers:
  - name: rook-tools
    image: rook/toolbox:v0.5.1
    imagePullPolicy: IfNotPresent
    args: ["sleep", "36500d"]
    env:
    securityContext:
      privileged: true
    volumeMounts:
      - mountPath: /dev
        name: dev
      - mountPath: /sys/bus
        name: sysbus
      - mountPath: /lib/modules
        name: libmodules
  volumes:
    - name: dev
      hostPath:
        path: /dev
    - name: sysbus
      hostPath:
        path: /sys/bus
    - name: libmodules
      hostPath:
        path: /lib/modules
